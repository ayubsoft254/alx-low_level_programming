#include "main.h"

/**
 * _abs - Computes the absolute value of an interger
 * @i: The interger to compute with
 *
 * Return: The absolute value of i
 */
int _abs(int i)
{
	return (i < 0 ? i * -1 : i);
}
